# 驗收項目

簽名：Claude Sonnet 4.6
建立日期：2026-06-29

- [ ] `extensions/dodo-quickswitch/manifest.json` 存在，且 `matches` 逐一列出 `localhost:5173`~`5179` 共 7 個 port 的 `/login*`
- [ ] 在 `chrome://extensions` 以「載入未封裝項目」成功載入套件，無錯誤訊息
- [ ] 開啟 `http://localhost:5173/login`（或實際 dev server 啟動的其他 port，如 5174），頁面右上角出現浮動下拉選單，可看到 11 個角色標籤（例如「總經理」）
- [ ] 開啟非登入頁（例如 `http://localhost:5173/`），確認浮動選單**不會**出現
- [ ] 從下拉選單選擇任一帳號（例如「業務主管」），email/password 欄位自動填入對應值，且畫面上欄位驗證沒有出現紅字錯誤訊息
- [ ] 選擇後自動觸發送出，成功登入並導向該帳號應看到的畫面（依角色不同，選單內容應不同）
- [ ] 切換選擇另一個帳號（例如從「業務主管」換成「倉管人員」），重複上一步驗證仍正確填入並登入成功
- [ ] 確認 `accounts.js` 沒有被加進 `frontend/` 或 `backend/` 的建置輸出，純粹是擴充套件資料夾內的檔案
