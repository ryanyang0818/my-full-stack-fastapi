# 待辦事項

> 最後更新:2026-06-25
> 本資料夾 (`docs/todo/`) 已設為 git ignore,僅本機個人筆記,不進版控。

---

## 🔴 主要:前端側邊欄接上後端 menus API(狀態:之後再做)

**現況**:前端側邊欄是**寫死的假資料** (`frontend/src/components/Sidebar/tree-data.ts` 的 `TREE_DATA`),跟後端的 RBAC 權限系統**完全沒接**。後端 `/api/v1/menus/tree` 會依角色/覆寫算出可見選單(已驗證可用),但前端沒呼叫它。

**注意**:寫死的選單跟 DB seed 的選單是**兩套不同設計**(寫死的有「報表中心」、無「人資/儀表板」;DB 有「人資/儀表板」、無「報表中心」),接 API 等於換掉整個選單來源。

**執行步驟**:

- [ ] 1. 重新生成前端 client(`scripts/generate-client.sh`,需後端在跑)→ 讓 `MenusService.readMyMenuTree` 存在(目前 client 是舊的,沒有 `MenusService`)
- [ ] 2. 新增 hook(可參考 A 版的 `useMenuTree.ts`)→ 用 react-query 打 `/menus/tree`
- [ ] 3. 改 `AppSidebar.tsx` / `TreeMenu.tsx` → 改吃 API 資料,不吃 `TREE_DATA`
- [ ] 4. icon 映射 → API 回傳字串(如 `"shopping-cart"`),前端用 Lucide 元件,需做 string → 元件 對應表

**動手前要先想清楚(建議用 brainstorming)**:
- icon 映射策略(維護一張 string→LucideIcon 表?)
- 要不要保留 `TREE_DATA` 當 API 失敗時的 fallback?
- 頁籤系統(tab store)怎麼跟「動態選單」整合?點 API 來的選單如何開頁籤?

---

## 🟡 次要/觀察項

- [ ] **把 AdventureWorks 資料庫移植回 B(要找時間做)**
  **現況:B 目前的環境沒有 AdventureWorks**(B 是全新 volume)。它原本在 A 的 volume `full-stack-fastapi-template_app-db-data`(368MB,推測但尚未實證),而且是**唯一不可重建**的資料(程式碼裡零紀錄、無重建腳本)。
  步驟:
  1. 起暫時容器**唯讀**確認 AdventureWorks 真的在 A 的 volume 裡、叫什麼名字
  2. 從 A `pg_dump` 匯出 AdventureWorks
  3. 匯入 B 的資料庫(或 B 的 postgres 實例)
  ⚠ 在成功移植/備份前,**A 的 volume 絕對不能有任何閃失**——它是現在唯一的一份。

- [ ] **auth 404 邊角(延後,有需要再修)**
  `backend/app/api/deps.py:43`:token 有效但使用者不存在時回 404,前端 `handleApiError` 只攔 401/403 → 不會自動登出。**正常運作不會發生**,但「管理員刪掉某個還登入中的使用者」會觸發。若日後真有使用者回報「被刪帳號後卡住」,把那行 `404` 改 `401` 即可。

- [ ] **`.env` 在 git 版控(小安全疑慮)**
  B 的 `.env` 被 git 追蹤,密碼(舊的 `12345678`)與 email 已進 git 歷史。本機開發無妨,日後若要公開 repo 需處理(改寫歷史 + 改用 `.env.example`)。

- [ ] **Docker 空間回收(可選)**
  Build Cache 約 11.6GB 可回收:`docker builder prune`。純編譯快取,安全。

---

## ✅ 本次已完成(2026-06-25)

- A 的後端 RBAC 程式碼導入 B、B 用全新 volume 啟動成功
- 結構(9 個 alembic 遷移)自動套用到位、6 表 + 6 視圖建好
- seed 資料依序灌入(role 11 / menu 34 / user_role 1 / role_menu 131 / override 1)
- 驗證 RBAC 端到端正確(admin 經 deny 覆寫後可見 33/34 選單)
